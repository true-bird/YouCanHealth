package com.mycompany.myapp.geustbook.service;

import java.util.HashMap;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.mycompany.myapp.geustbook.dao.GuestbookDaoImpl;
import com.mycompany.myapp.geustbook.dto.GuestbookDto;

@Service //서비스임을 선언. 특별한 기능은 없다.
public class GuestbookServiceImpl implements IGuestbookService{

	@Autowired //자동주입
	private GuestbookDaoImpl guestbookDaolmpl;

	public List<GuestbookDto> selectGuestbookAllList(){
		return guestbookDaolmpl.selectGuestbookAllList();
	}

	public void insertGuestbook(HashMap<String, String> paramMap) {
	// TODO Auto-generated method stub
	
	}
}
